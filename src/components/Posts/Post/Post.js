import React from 'react';
import useStyles from './styles'

const Post = () => {
    const classes = useStyles()
    return (
        <div>
            <h2>Single Post</h2>
        </div>
    );
};

export default Post;